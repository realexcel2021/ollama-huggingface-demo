apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hugging-face-chat-ui.fullname" . }}-test-connection"
  labels:
    {{- include "hugging-face-chat-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hugging-face-chat-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
