# Hugging Face Chat UI Deployment on Kubernetes

This project demonstrates how to deploy the huggging face chat UI on kubernetes. It also shows how a model can be deployed on kubernetes using ollama as the model's API interface.